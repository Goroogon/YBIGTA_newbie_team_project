name: Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ybigta-app:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ybigta-app:${{ github.sha }}

  deploy-to-ec2:
    name: Deploy to EC2
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      # TODO(AWS 담당자): EC2 접속 후 docker pull + 컨테이너 재실행 단계 작성
      # 필요한 secrets 예시: EC2_HOST, EC2_SSH_KEY, EC2_USER
      - name: Placeholder
        run: echo "AWS 담당자가 이어서 작성"
